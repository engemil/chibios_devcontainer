{
    "settings.project.name": "project", // The name of your project (e.g. "project")
    "settings.project.folder": "${workspaceFolder}/project", // The path to your project folder (e.g. "/workspace/project")
    "settings.project.compiledElfFile": "${workspaceFolder}/project/build/ch.elf", // The path to the compiled .elf file for your project (e.g. "/workspace/project/build/ch.elf")
    "settings.project.svdFile": "${workspaceFolder}/project/STM32C071.svd", // .svd file (e.g. "/workspace/project/STM32F401.svd")
    "settings.openocd.interface": "interface/stlink.cfg", // OpenOCD Debugger interface file (e.g. "interface/stlink.cfg")
    "settings.openocd.board": "board/st_nucleo_c0.cfg", // OpenOCD Board file (e.g. "board/st_nucleo_f4.cfg")
    "settings.project.device": "STM32C071RB" // Chip name (e.g. "STM32C071RB")
}
